# Puzzpals client

## Puzzle file format

A puzzle can be exported from the editor as a [`Grid`](../packages/puzzle-models/README.md#grid) object in a JSON file. The `solution` field is omitted if the solution is excluded.
