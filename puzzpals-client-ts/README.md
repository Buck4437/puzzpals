# Puzzpals client

## Puzzle file format

A puzzle can be exported from the editor as a [`Grid`](../packages/puzzle-models/README.md#grid) object in a JSON file. `solution` is `undefined` if the solution is excluded.
